version: '3.7'
services:
  backend:
    build: .
    container_name: microservice-emails
    ports:
      - 8001:8000
    volumes:
      - .:/app
    command: 'php artisan queue:work'
    networks:
      - proxynet

networks:
  proxynet:
    name: pub_sub_network
    external: true
